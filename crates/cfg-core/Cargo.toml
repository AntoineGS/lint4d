[package]
name = "cfg-core"
version = "0.1.0"
edition = "2021"
description = "Language-agnostic control flow graph with interprocedural analysis"
license = "MIT"

[dependencies]
petgraph = "0.7"
serde = { version = "1", features = ["derive"], optional = true }

[features]
default = []
serde = ["dep:serde", "petgraph/serde-1"]
