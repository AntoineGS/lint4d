[package]
name = "cfg-pascal"
version = "0.1.0"
edition = "2021"
description = "Pascal/Delphi control flow graph builder using tree-sitter"
license = "MIT"

[dependencies]
cfg-core = { path = "../cfg-core" }
tree-sitter = "0.24"
tree-sitter-pascal = "0.10"

[dev-dependencies]
cfg-core = { path = "../cfg-core" }
